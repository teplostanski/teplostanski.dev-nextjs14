export type PostMetadata = {
  title: string
  description: string
  prep_time: string
  cook_time: string
  slug: string
  img: string
  bio: string
  content: string
  locale: 'ru' | 'en' // Добавляем сюда locale
}
